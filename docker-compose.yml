version: "3.8"

services:
  web:
    build: .
    container_name: men-fashion-app
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: npm start
